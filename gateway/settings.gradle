pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'medilabo'

include 'gateway'
include 'your-other-project-name' // ← Add your other project here
